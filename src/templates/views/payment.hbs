<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>payment</title>
</head>

<body>
    {{>navbar}}
    <div class="m-4">
        {{#if msg}}
        <div class="alert alert-danger d-flex align-items-center" role="alert">
            <div>
                {{msg}}
            </div>
        </div>
        {{/if}}
        <form method="post" action="/cart/payment">
            <div class="container h-100 py-5">
                <section class="h-100" style="background-color: #eee;">

                    <div class="container h-100 py-5">
                        <div class="row d-flex justify-content-center align-items-center h-100">
                            <div class="col-10">
                                <div class="d-flex justify-content-between align-items-center mb-4">
                                    <h2 class="fw-normal mb-0 text-black">Select Address</h2>
                                </div>
                                {{#each address}}
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="selectedAddress"
                                        id="{{this.length}}" value="{{this}}" checked>
                                    <label class="form-check-label" for="{{this.length}}">
                                        {{this}}
                                    </label>
                                </div>
                                {{/each}}
                                <a href="/addAddress">
                                    <button type="button" class="mt-4 btn btn-outline-primary">Add New Address</button>
                                </a>
                            </div>
                        </div>
                    </div>
                </section>
                <section class="h-100 mt-2" style="background-color: #eee;">

                    <div class="row d-flex justify-content-center align-items-center h-100">
                        <div class="col-10">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <h2 class="fw-normal mb-0 text-black">Payment</h2>
                            </div>
                            <h5>bill amount : {{sum}}</h5>
                            <input type="hidden" name="sum" value="{{sum}}">
                            {{!-- <label for="exampleInputEmail1">Enter Amount</label>
                            <input type="number" min="{{sum}}" max="{{balance}}" class="form-control"
                                id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Enter Amount"
                                name="amount" /> --}}
                            <label for="exampleInputEmail1">Enter Password</label>
                            <input type="password" class="form-control" id="exampleInputEmail1"
                                aria-describedby="emailHelp" placeholder="Enter Password" name="payPassword" />
                            <button type="submit" class="mt-4 mb-4 btn btn-outline-success">Pay</button>
                        </div>
                    </div>
                </section>
            </div>
        </form>
    </div>
    {{>animation}}
</body>

</html>